// PLACEHOLDER (wave 1). Replace the whole body of this file with the real
// screen; do not touch src/router.tsx — the route is already registered.

import { Placeholder } from "../Placeholder";

export default function ProjectsScreen() {
  return <Placeholder name="Projects & Repositories" spec="Handoff § 3. Projects & Repositories — 3-up card grid plus the dashed “Connect a repository” tile." />;
}
