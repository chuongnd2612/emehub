// PLACEHOLDER (wave 1). Replace the whole body of this file with the real
// screen; do not touch src/router.tsx — the route is already registered.

import { Placeholder } from "../Placeholder";

export default function AuthScreen() {
  return <Placeholder name="Authentication" spec="Handoff § 7. Authentication — Single sign-on, Sessions, API keys and Login providers." />;
}
