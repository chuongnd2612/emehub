// PLACEHOLDER (wave 1). Replace the whole body of this file with the real
// screen; do not touch src/router.tsx — the route is already registered.

import { Placeholder } from "../Placeholder";

export default function ProjectDetailScreen() {
  return <Placeholder name="Project detail" spec="Handoff § 3. Projects & Repositories — detail state: header card and the Overview / Project knowledge / Repository / Agents / Settings tabs." />;
}
