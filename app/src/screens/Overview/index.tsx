// PLACEHOLDER (wave 1). Replace the whole body of this file with the real
// screen; do not touch src/router.tsx — the route is already registered.

import { Placeholder } from "../Placeholder";

export default function OverviewScreen() {
  return <Placeholder name="Overview" spec="Handoff § 2. Overview — greeting row, product cards, 4-up KPI tiles, activity feed, integration strip, top-3 projects." />;
}
