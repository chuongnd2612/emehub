// Barrel for the shell's floating overlays. Every one of these portals to
// document.body with fixed positioning (CLAUDE.md › Frontend conventions).

export { ClaudeCredentialChip } from "./ClaudeCredentialPopover";
export { CommandPalette } from "./CommandPalette";
